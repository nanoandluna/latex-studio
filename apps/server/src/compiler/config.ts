/** Shared compiler-module constants and small types. */

export const BUILD_DIR_NAME = '.build';
export const BUILD_TIMEOUT_MS = 180_000;

export type EngineChoice = 'xelatex' | 'pdflatex' | 'lualatex';
