# chinese-thesis fixture

`ctexbook` 章节式中文论文（`\chapter` + `\section` 层级、图表交叉引用），
同时覆盖结构解析器的 chapter 级别。

Requires: **XeLaTeX**（ctex 宏包）+ bibtex。
Expected: build 成功；PDF 含章级标题；Outline 可解析 chapter/section 两级。
