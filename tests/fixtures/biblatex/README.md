# biblatex fixture

Full **BibLaTeX + Biber** workflow with `style=numeric,sorting=none`,
`\autocite` / `\textcite` / `\parencite` commands and multi-key citations.

Requires: any engine + **biber**.
Expected: build succeeds; no undefined-citation diagnostics survive;
bibliography printed.
