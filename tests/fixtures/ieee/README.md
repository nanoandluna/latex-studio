# ieee fixture

`\documentclass[conference]{IEEEtran}` + `cite` package + classic
`\bibliography` workflow.

Requires: any engine + bibtex (MiKTeX/TeX Live provide IEEEtran).
Expected: build succeeds; both citations resolve.
