# beamer fixture

Presentation class compatibility check (`\documentclass{beamer}`, frames).
Requires: any engine (xelatex recommended; pdflatex fine too).
Expected: `main.pdf` generated successfully.
